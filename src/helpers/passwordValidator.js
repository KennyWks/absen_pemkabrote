export function passwordValidator(password) {
  if (!password) return 'Password wajib diisi.';
  // if (password.length < 18) return 'Password tidak valid.';

  // if (password.length != 18) return 'Password tidak valid.';
  return '';
}
